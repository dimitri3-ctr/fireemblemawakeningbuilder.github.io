mouseClickMethod(select_char);
getWidth();
getHeight();
var chrom = new Text("Chrom", "30pt Arial");
var x = getWidth() / 2;
var y = getWidth() / 2;
chrom.setPosition(x, y);
chrom.setColor(Color.blue)
add(chrom)
